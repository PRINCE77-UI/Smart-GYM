# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/roleAuth
# Uncomment and replace the above with your actual MongoDB URI, or keep it commented to use the local fallback.
JWT_SECRET=your_secret
PORT=5000